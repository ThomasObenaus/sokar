version: "3"
services:
  service_test:
    build:
      context: .
      dockerfile: test.Dockerfile
    command: ["make", "test.endpoints"]
    depends_on:
      - sokar
    network_mode: host
  sokar:
    build:
      context: .
      dockerfile: endpoints.sokar.Dockerfile
    network_mode: host
