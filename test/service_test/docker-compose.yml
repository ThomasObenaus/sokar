version: "3"
services:
  service_test:
    build:
      context: .
      dockerfile: Dockerfile
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    network_mode: host
#  sokar:
#    build:
#      context: ../../
#      dockerfile: ci/Dockerfile
#    deploy:
#      replicas: 1
#      restart_policy:
#        condition: on-failure
#    network_mode: host
networks:
  webnet:
