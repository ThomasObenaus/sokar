version: "3"
services:
  service_test:
    build:
      context: .
      dockerfile: test.Dockerfile
    depends_on:
      - sokar
    network_mode: host
  sokar:
    build:
      context: .
      dockerfile: sokar.Dockerfile
    network_mode: host
